/**
 * Шкала слоёв.
 *
 * Раньше шапка стояла на z-1000, а модалки и выдвижные панели — на z-50,
 * то есть все они открывались ПОД шапкой. Кнопка закрытия, если попадала
 * в верхние 73 пикселя, оказывалась недоступной для клика.
 *
 * Правило простое: всё, что перекрывает страницу, должно быть выше шапки.
 */
export const Z = {
  /** Липкая шапка сайта. */
  header: 'z-[1000]',
  /** Затемнение под модалкой или панелью. */
  overlay: 'z-[1100]',
  /** Сама модалка, выдвижная панель, выпадающее меню. */
  modal: 'z-[1100]',
  /** Панель редактора и её списки — поверх модалок. */
  editor: 'z-[1500]',
  /** Тосты и уведомления — выше всего. */
  toast: 'z-[2000]',
} as const;
